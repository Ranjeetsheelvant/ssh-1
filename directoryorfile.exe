#!/bin/sh 
#using -d is use to check wether directory or not 
if [ -d $1 ]
then
	echo "the provide argument is the file"
elif [ -f $1 ]

then
echo "the provided argument is file"
else
	echo "the given argument does not exist on the file directory"
fi

